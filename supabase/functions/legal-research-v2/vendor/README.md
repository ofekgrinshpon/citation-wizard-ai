# Vendored primitives

Frozen copies of low-level V1 helpers (HTTP profiles, extraction, Hebrew/docket/statute
normalization, title hygiene, structured validation). Edge deploys bundle only the
function's own directory, so V2 owns these copies outright. Nothing here is V1
orchestration: no pools, ranking, roles, modes or rescue stages.
